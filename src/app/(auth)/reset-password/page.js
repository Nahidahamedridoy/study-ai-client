import ResetPasswordView from './ResetPasswordView';

export const metadata = { title: 'Reset Password | StudyMate AI' };

export default function ResetPasswordPage() {
    return <ResetPasswordView />;
}
